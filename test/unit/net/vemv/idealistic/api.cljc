(ns unit.net.vemv.idealistic.api
  (:require
   #?(:clj [clojure.test :refer [deftest testing are is use-fixtures]] :cljs [cljs.test :refer-macros [deftest testing is are] :refer [use-fixtures]])
   [net.vemv.idealistic.api :as sut]))

(deftest fixme
  (is (= 1 2)))
